<%= turbo_stream.replace("claim_type_#{@claim_type.public_id}", @claim_type.decorate) %>
<%= turbo_stream.update dom_class(ClaimType, "form") do %>
  <%= render "link_new" %>
<% end %>
<%= turbo_stream
  .invoke(
    "document.dispatchEvent",
    args: ["turbo-notification:show", { detail: { message: @message } }],
  ) %>
