<%= turbo_stream.replace("leave_type_#{@leave_type.public_id}", @leave_type.decorate) %>
<%= turbo_stream.update dom_class(LeaveType, "form") do %>
  <%= render "link_new" %>
<% end %>
<%= turbo_stream
  .invoke(
    "document.dispatchEvent",
    args: ["turbo-notification:show", { detail: { message: @message } }],
  ) %>
