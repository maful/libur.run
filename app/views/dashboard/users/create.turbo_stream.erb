<%= turbo_stream.reload_turbo_frame "users-table", src: users_path %>
<%= turbo_stream
  .invoke("document.dispatchEvent", args: ["turbo-modal:close"])
  .invoke(
    "document.dispatchEvent",
    args: ["turbo-notification:show", { detail: { message: "User was successfully created." } }],
  ) %>
